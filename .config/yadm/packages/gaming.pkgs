# Gaming stack — OPT-IN. Installed only when gaming is enabled for the machine:
#   yadm config local.gaming true      (bootstrap also prompts on desktop/laptop)
#   BOOTSTRAP_GAMING=1 yadm bootstrap   (force-enable, non-interactive)
#
# Mirrors the CachyOS gaming guide: https://wiki.cachyos.org/configuration/gaming/
# Repo-only. Multilib is enabled by default on CachyOS; on plain Arch the
# bootstrap uncomments [multilib] for you (the lib32-* deps below need it).
# GPU-side 32-bit drivers (lib32-nvidia-utils / lib32-vulkan-radeon) live in the
# class lists (desktop.pkgs / laptop.pkgs), not here — they differ per machine.
#
# PORTABILITY: cachyos-gaming-applications lives ONLY in the [cachyos] repo —
# it is not in Arch and not in the AUR. On plain Arch the bootstrap offers to
# add [cachyos]; if you decline, bootstrap skips this list and prints the
# Arch-native equivalent to install by hand:
#   steam lutris heroic-games-launcher gamescope goverlay mangohud lib32-mangohud
#   wine winetricks protontricks umu-launcher vulkan-tools

# --- CachyOS gaming bundle (the guide's one-shot meta) ---
# cachyos-gaming-applications pulls the whole recommended set:
#   steam, lutris, heroic-games-launcher, gamescope, goverlay,
#   mangohud + lib32-mangohud, AND cachyos-gaming-meta, which adds
#   proton-cachyos-slr, umu-launcher, protontricks, wine-cachyos-opt,
#   winetricks, vulkan-tools + the lib32 runtime libs games expect.
cachyos-gaming-applications

# --- Feral GameMode (the meta deliberately omits it; some titles still ask for
#     it, and `gamemoderun %command%` is a handy per-game perf toggle) ---
#
# NOTE: the CachyOS guide warns against running gamemode and ananicy-cpp at the
# same time — both rewrite process niceness and fight each other. ananicy-cpp is
# a dependency of cachyos-settings so it cannot simply be removed; stop the
# service instead:  sudo systemctl disable --now ananicy-cpp
gamemode
lib32-gamemode

# --- game-performance's backend ---
# CachyOS ships a `game-performance` wrapper (from cachyos-settings) that flips
# the power profile to `performance` for the life of the game. It drives
# power-profiles-daemon over D-Bus, so without this package the wrapper the
# guide tells you to put in every launch option silently does nothing.
#
# Also declared in class/laptop.pkgs, which wants profile switching whether or
# not that machine games. Duplicate entries across lists are deduped at install.
power-profiles-daemon

# HDR: gamescope (pulled above) is the HDR path — run a title with
#   gamescope -f --hdr-enabled -- %command%
# and give the display an HDR monitor line in monitors.conf (see hypr/gaming.conf).
