# Generated by nwg-displays on 2026-05-09 at 08:28:18.
# Hand-edited since — see the DP-3 notes below. nwg-displays REGENERATES this
# file and will strip both changes; re-apply them after any run of it.

monitor=HDMI-A-2,2560x1440@143.97,0x504,1.0
monitor=HDMI-A-2,transform,1
monitor=DP-2,3440x1440@164.9,1440x0,1.0,bitdepth,10

# DP-3 — scale deviates from what nwg-displays generated (1.33 -> 1.3333333):
# wp_fractional_scale_v1 sends scale to clients as an integer number of 120ths.
# 1.33 is 159.6/120, not representable, so clients were told 160/120 = 1.333333
# while the compositor laid out at 1.33. 1.3333333 rounds to exactly 160/120 AND
# divides 3840x2160 into a whole 2880x1620, so windows stop landing on
# fractional pixel boundaries (1.33 gave 2887.2x1624.06).
#
# --- If corner corruption comes back, drop this line to @119.88 -------------
# 4K240 needs a 2331.75 MHz pixel clock: 99.6% of this panel's rated 2340 MHz
# ceiling, and it requires DSC over DisplayPort. This link has already shown one
# episode of scanout corruption at 240 — visible on the panel but absent from a
# gpu-screen-recorder capture, which is the signature of a link/scanout fault
# rather than a compositor bug. It cleared after a 240->120->240 mode cycle,
# i.e. after the DP link re-trained, so the underlying margin is still thin.
# 119.88 (1186.81 MHz) was rock solid; there is no intermediate 4K mode on this
# panel, only 240 and 120. The real cure is a VESA-certified DP40/DP80 cable.
# Note bitdepth,10 makes 240Hz cost more bandwidth here than mango's 8-bit ask.
monitor=DP-3,3840x2160@239.99,1440x1440,1.3333333,bitdepth,10
