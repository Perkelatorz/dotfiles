# Role: laptop. Anything EVERY laptop wants, regardless of which one it is.
#
# GPU packages deliberately live in host/<name>.pkgs, not here — that is the
# thing two laptops are most likely to disagree about, and duplicating the rest
# of this file to fork the class would be the wrong trade.

# --- Power ---
power-profiles-daemon
